import React from 'react';
import './index.css';

import createBrowserHistory from 'history/createBrowserHistory';

const history = createBrowserHistory();

ReactDOM.render(
  <div>
				<h2>Welcome to Loin page</h2>
			</div>,
  document.getElementById('root')
);
